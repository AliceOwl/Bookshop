<h1>Line Items</h1>

<table class="responsive nowrap table table-hover" id="dttb-line_items" data-sort="true" data-source="<%= url_for(format: :json)%>">
  <thead>
    <tr>
      <th data-data="product">Product</th>
      <th data-data="cart">Cart</th>
      <th data-data="url" data-orderable="false" data-class-name="all" data-searchable="false" class="skip-export" width="100px"></th>
    </tr>
  </thead>

</table>

<br>

<%= link_to 'New Line Item', new_line_item_path %>